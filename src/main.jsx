import React from "react";
import { createRoot } from "react-dom/client";
import TransferRoom from "./App.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TransferRoom />
  </React.StrictMode>
);
